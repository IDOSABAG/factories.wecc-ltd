// Stub entities - BASE44 dependency removed
// Add your own entity implementations here if needed

export const Query = {
  // Add query methods as needed
};

export const User = {
  // Add user methods as needed
};
